- var socketio = '//cdnjs.cloudflare.com/ajax/libs/socket.io/0.9.16/socket.io.min.js'
- var $ = '//cdnjs.cloudflare.com/ajax/libs/zepto/1.1.2/zepto.min.js'

doctype html
html(lang="en_CA")
  head
    meta(charset="UTF-8")
    title Ludwig Demo

    != css('reset')
    != css('main')

    //- I'll include these scripts later.
      script(src=socketio)
      script(src=$)
      != js('main')

  body
    header.main-header
      h1.main-title
        span.ludwig Ludwig
        | 
        span.demo Demo

    .greeting

    .workspace
      .entry#main-input
        textarea.extravagant-text-entry(
          autofocus, placeholder="Type here and press enter")
          | Barack Obama plans to invade the Taco Bell empire.

      .results
        .placeholder.
          No results to show.

    footer.main-footer
      p.copyright.
        2014 © Eddie Antonio Santos. BSD Licensed.

    template#t-result
      .result
        dl.meta
          dt Request ID
          dd.request-id undefined
          dt Internal ID
          dd.internal-id undefined
          dt Turnaround time (ms)
          dd.tatime undefined
        .content undefined

    //-
      .result
        dl.meta
          dt Request ID
          dd.request-id 3
          dt Internal ID
          dd.internal-id 89
          dt Turnaround time
          dd.tatime 89 ms
        .content.nlptext.
          [[PER Barack Obama]] plans to invade the [[ORG Taco Bell]] empire.

